{
    "name": "@iroha2/crypto-test-web",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "dev-server": "vite --port 3030",
        "build": "vite build",
        "preview": "vite preview --port 3030",
        "cy:run": "cypress run --headless",
        "test": "pnpm build && start-server-and-test preview http://localhost:3030 cy:run"
    },
    "dependencies": {
        "@iroha2/crypto": "link:../..",
        "cypress": "^8.5.0",
        "hada": "^0.0.8",
        "start-server-and-test": "^1.14.0",
        "vite": "^2.5.10"
    }
}
