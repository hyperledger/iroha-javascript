{
    "name": "@iroha/crypto",
    "version": "0.1.0",
    "scripts": {
        "build:wasm": "pnpm build:wasm-web && pnpm build:wasm-node",
        "build:wasm-web": "wasm-pack build -d pkg-web -t web ./ursa-wasm",
        "build:wasm-node": "wasm-pack build -d pkg-node -t nodejs ./ursa-wasm"
    },
    "devDependencies": {
        "@iroha/eslint-config": "workspace:^0.1.0",
        "@iroha/prettier-config": "workspace:^0.1.0",
        "eslint": "^7.27.0",
        "prettier": "^2.3.0",
        "prettier-eslint": "^12.0.0",
        "typescript": "^4.3.2",
        "vite": "^2.3.6"
    },
    "prettier": "@iroha/prettier-config",
    "eslintConfig": {
        "extends": [
            "@iroha/eslint-config"
        ]
    }
}
