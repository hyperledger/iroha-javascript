# @iroha/prettier-config

Prettier config shared along the projects.

### Usage

Install:

```sh
pnpm add @iroha/prettier-config
```

Use:

```json
// package.json
{
  "prettier": "@iroha/prettier-config"
}
```
