{
  // for some reason cypress config fails to load if we extend this
  // "extends": ["../../../../../tsconfig.base.json"],
  "compilerOptions": {
    // essentials for vue-tsc to work
    "skipLibCheck": true,
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "Bundler"
  },
  "include": ["./src/**/*.ts", "./src/**/*.vue"]
}
